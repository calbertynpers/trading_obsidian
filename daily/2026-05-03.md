# Sunday, May 03, 2026


## AWS Deployment Framework — Session Log

### Work completed
- Discovered existing EC2 instance `i-0d3a293742aa3ad5c` in eu-north-1 (t3.small, AL2023)
- Verified SSH access with `clint_dev_personal.pem`
- Ran `ec2-setup.sh` on instance — installed Python 3.14.4, uv 0.11.8, git 2.50.1, created `/opt/trading/bds/` directory structure, configured log rotation
- Created `scripts/ec2-setup.sh` (idempotent bootstrap) and `scripts/ec2-teardown.sh` (clean teardown)
- Created `templates/ec2-base.yml` (CloudFormation for new EC2 + EIP + SG + IAM)
- Created `.kiro/steering/aws-deployment-framework.md` (architecture decisions, naming, tagging)
- Updated `CLAUDE.md` for project handoff

### Tickets created/updated
- **ADF-001** → 🟢 Done (SSH key confirmed)
- **ADF-002** → 🟡 In Progress (setup script run, CF template created, remaining: EIP, SG, IAM, tags)
- **ADF-003** → 🔴 Open (Secrets Manager — Binance API + DB creds, region corrected to eu-north-1)
- **ADF-004** → 🔴 Open (RDS PostgreSQL db.t4g.micro, BDS schema defined)
- **ADF-005** → 🔴 Open (BDS deployment — asyncpg migration + systemd)
- **ADF-006** → 🔴 Open (CI/CD pipeline — CodeBuild + CodeDeploy)
- **ADF-007** → 🔴 Open (Position monitoring dashboard — S3 + CloudFront + FastAPI)
- Ticket Index updated with full ADF section and ADF prefix added to Service Prefixes

### Decisions made
- EC2-first approach (not Fargate) — cheapest, zero code changes for initial deploy
- eu-north-1 confirmed fine for Binance API (global endpoint, no geo-restriction)
- RDS PostgreSQL from the start (not SQLite on EBS) — concurrent access for future services
- Separate Binance API key for EC2 (not shared with local)
- Project manifest pattern (`deploy.yml`) for future service onboarding

### Next steps
1. Complete ADF-002: Elastic IP, security group hardening, IAM instance profile
2. ADF-003: Secrets Manager setup
3. ADF-004: RDS PostgreSQL provisioning
4. ADF-005: BDS deployment (asyncpg migration is the main code work)
